<%= simple_form_for([@event, @event_user]) do |f| %>
    <%= f.input :motivation, placeholder: "Let the host know, why you want to join", class: "motivation-text"%>
    <%= f.submit "Join this event!", class:"motivation-btn", id: "anchor"%>
  <% end %>
