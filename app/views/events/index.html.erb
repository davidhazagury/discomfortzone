
<h1>Customize your Adventure</h1>
<br>
<!-- <div class="buttons">
  <div class="btn btn-success btn-block">Easy-Going</div>
  <div class="btn btn-primary btn-block">Enterntaining</div>
  <div class="btn btn-danger btn-block">Crazy</div>
  <div class="btn btn-secondary btn-block">Sporty</div>
</div> -->
<br>
<br>

<!-- The searchbar that was there before -->
<div class="container">
  <%= simple_form_for :search, url: events_path, method: :get do |f| %>
    <div class="search-form-control form-group">
      <input class="form-control string required" type="text" name="search[query]" placeholder="Location" id="search_query" />
      <button name="button" type="submit" class="btn btn-flat">
        <i class="fas fa-search"></i> Search
      </button>
    </div>
  <% end %>
</div>
<br>

<!-- Harry Potter Search Feature -->
<%= form_tag events_path, method: :get do %>
  <%= text_field_tag :query,
    params[:query],
    class: "form-control",
    placeholder: "Find an adventure"
  %>
  <%= submit_tag "Search", class: "btn btn-flat" %>
<% end %>

<br>

<!--THIS IS AN PERSONALIZE AVATAR-->
<% if @current_user.photo.attached? %>
<div style="width: 50px; border-radius: 50%;"><%= cl_image_tag @current_user.photo.key %></div>
<% end %>
<h2>Your Results: </h2>
<div class="cards-container">
    <% @events.each do |event| %>
      <div class="cards">
      <%= link_to event_path(event) do  %>
        <% if event.photo.attached? %>
          <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path event.photo.key %>');">
            <%= event.start_time %>
          </div>
        <% else %>
          <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://source.unsplash.com/1600x900/?map)">
            <%= event.start_time %>
            <%= event.title %>
          </div>
        <% end %>
        <div class="cards-description">
          <div class="cards-text">
            <h3> Event: <%= event.title %> </h3>
            <h4> Location: <%= event.address %></h4>
            <h4> Creator: <%= event.user.first_name %> (<%= event.spots_left %> spots left)</h4>
          </div>
          <div class="favicon">
            <i class="fa fa-heart" aria-hidden="true"></i>
          </div>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
