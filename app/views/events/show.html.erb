<div class="container">
  <p>Here's the map below. let's keep it at the bottom until we decide where to put it!</p>
  <div id="map"
     style="width: 100%; height: 600px;"
     data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>
